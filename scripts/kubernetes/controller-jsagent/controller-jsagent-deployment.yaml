apiVersion: apps/v1beta2 # for versions before 1.8.0 use apps/v1beta1
kind: Deployment
metadata:
  name: controller-jsagent
spec:
  selector:
    matchLabels:
      app: controller-jsagents
  replicas: 1 # Update the replicas from 2 to 4
  template:
    metadata:
      labels:
        app: controller-jsagent
    spec:
      restartPolicy: Always
      initContainers:
      - name: system-init
        imagePullPolicy: Always
        command: [jspython]
        args: ['configure-system.py', '--roles', 'controller,node']
        image: openvcloud/cb_master
        workingDir: /var/run/scripts/system
        volumeMounts:
        - name: system-hrd
          mountPath:  /opt/cfg/system/
        - name: apps-hrd
          mountPath:  /opt/cfg/apps/
        - name:  system-config
          mountPath:  /opt/cfg/system/system-config.yaml
          subPath: system-config.yaml
      containers:
      - name: jsagent
        imagePullPolicy: Always
        command: [jspython]
        args: ['jsagent.py', '-i', 'main']
        image: openvcloud/cb_master
        workingDir: /opt/jumpscale7/apps/jsagent/
        ports:
        - containerPort: 4446
        volumeMounts:
        - name: system-hrd
          mountPath: /opt/jumpscale7/hrd/system/
        - name:  apps-hrd
          mountPath: /opt/jumpscale7/hrd/apps/
      volumes:
      - name: system-hrd
        emptyDir: {}
      - name: apps-hrd
        emptyDir: {}
      - name: system-config
        configMap:
          name: system-config



